# sisinformaticos

git clone "https://github.com/Cristiancj99/sisinformaticos.git"
git add readme 
git commit -m "primer commit"s
git push -u master
creamos las cosas en el repositorio local

touch .gitignore.

dentro de el archivo ponemos el nombre del archivo que queremos.
realizamos un git add
git commit -m " git ignore"

git tag -a v0.1 -m 'v0.1'

para comprobar si está subido poner git tag.


